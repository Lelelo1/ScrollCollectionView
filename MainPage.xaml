<?xml version="1.0" encoding="utf-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="ScrollCollectionViewTest.MainPage"
    xmlns:custom="clr-namespace:Namespace;assembly=ScrollCollectionView"
    xmlns:l="clr-namespace:ScrollCollectionViewTest"
    >
    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="Welcome to Xamarin.Forms!" HorizontalOptions="Center" VerticalOptions="CenterAndExpand" />
        <!--
        <CollectionView ItemsSource="{Binding TestList}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding .}"/>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        -->
        <custom:ScrollCollectionView ItemsSource="{Binding TestList}">
            <custom:ScrollCollectionView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding .}"/>
                </DataTemplate>
            </custom:ScrollCollectionView.ItemTemplate>
        </custom:ScrollCollectionView>
        <l:TestControl x:Name="testControl" MyText="hej"/>
        <Button Text="Add" BackgroundColor="BlueViolet" Clicked="Button_Clicked"/>
    </StackLayout>
</ContentPage>